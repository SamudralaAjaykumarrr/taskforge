# Environment / secrets
.env
.env.local

# Build outputs
bin/
dist/
*.exe

# Locally go-installed build tooling (e.g. cyclonedx-gomod; see Makefile's
# sbom target)
.tools/

# Test / coverage
coverage.out
*.coverprofile

# Temporary files
tmp/
*.log

# Editor / OS
.vscode/
.idea/
.DS_Store

# taskforge-autopilot's local, resumable run state and captured logs (see
# docs/autopilot.md) -- machine/run-local, never committed.
.taskforge-autopilot/
